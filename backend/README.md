# Pollution Tracker — Backend

This folder contains the Express + Mongoose backend for the Pollution Tracker project.

Quick start:

1. cd backend
2. npm install
3. Create a `.env` with `MONGODB_URI` (and other secrets as needed)
4. npm run dev

API endpoints are served under `/api`.

License: (add your license here)