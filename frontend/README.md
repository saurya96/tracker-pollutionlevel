# Pollution Tracker — Frontend

This folder contains the Vite + React frontend for the Pollution Tracker project.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

Build for production:

1. npm run build
2. Serve `dist/` with a static server

License: (add your license here)